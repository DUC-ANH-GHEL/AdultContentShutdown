{
  "type": "object",
  "properties": {
    "serviceUrl": {
      "title": "Local violation endpoint",
      "description": "The local Windows service endpoint that receives violation reports.",
      "type": "string"
    },
    "healthUrl": {
      "title": "Local health endpoint",
      "description": "The local Windows service health endpoint used by the popup.",
      "type": "string"
    },
    "token": {
      "title": "Local service token",
      "description": "Per-install token generated by the Windows service installer.",
      "type": "string"
    }
  }
}
